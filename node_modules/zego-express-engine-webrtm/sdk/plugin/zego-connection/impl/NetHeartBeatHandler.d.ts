import { ZegoLogger } from '../util/logger';
import { StreamManager } from './stream/StreamManager';
import { StateCenter } from './stateCenter';
export declare class NetHeartBeatHandler {
    private streamManager;
    private logger;
    private stateCenter;
    private hbTimer;
    private hbInterval;
    private hbTimeout;
    private hbTimeoutMaxCount;
    private tryHbCount;
    private hbStream;
    constructor(streamManager: StreamManager, logger: ZegoLogger, stateCenter: StateCenter);
    init(msg: {
        data: {
            logic_hb_interval: number;
            logic_hb_timeout: number;
        };
    }): void;
    start(): void;
    netHbLogout(err: any): void;
    reset(): void;
}
