export declare class Crypto {
    private get publicKey();
    private key1;
    private iv1;
    private key2;
    private iv2;
    /**
     * 加密 ArrayBuffer
     *
     * @param body 需要加密的 ArrayBuffer
     */
    encrypt(body: Uint8Array): Uint8Array;
    /**
     * 解密 ArrayBuffer
     *
     * @param body 需要解密的 ArrayBuffer
     */
    decrypt(body: Uint8Array): Uint8Array;
    /**
     * 生成 K1 签名，使用内置的公钥进行 RSA 签名
     *
     * 登录时发送给服务器
     */
    generateK1Sign(): Uint8Array;
    /**
     * AES 用密钥 k1 解密服务器的 k2Sign
     *
     * 登录后的请求 body，用 k2 加密
     */
    decryptK2Sign(k2Sign: Uint8Array): void;
    /**
     * 生成 16 位随机字符串
     */
    generateNonce(): string;
    md5(str: string): string;
    md5Bytes(bytes: Uint8Array): Uint8Array;
    private wordArray2Uint8Array;
    private uint8Array2WordArray;
}
